version: '3.8'
services:
  postgres:
    image: postgres:15-alpine
    container_name: task_management_dga_db
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
